#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define parth                         \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);                    \
    cout.tie(NULL)

int main()
{
    parth;
    ll q;
    cin >> q;
    while (q--)
    {
        string k;
        cin >> k;
        int numOfDig = k.size();
        }
    return 0;
}